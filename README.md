# Dotfiles

## Usage

- Clone this repo.
- go to `dotfiles` dir and make `init_dnf_workspace.sh` executable. ex: `chmod +x init_dnf_workspace.sh`.
- run `./init_dnf_workspace.sh` to start the script.
