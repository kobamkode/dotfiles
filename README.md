# Dotfiles

- Pull this repo with chezmoi command:

```sh
sh -c "$(curl -fsLS get.chezmoi.io)" -- init --apply kobamkode
```

- Make dotfiles/bootstrap.sh executable

```sh
sudo chmod +x dotfiles/bootstrap.sh
```

- Run dotfiles/bootstrap.sh

 ```sh
./dotfiles/bootstrap.sh
```
